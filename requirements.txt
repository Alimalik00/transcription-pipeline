# Core API framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Transcription engine (local, no API key needed)
openai-whisper==20231117
setuptools
# Audio format handling and chunking
pydub==0.25.1

# Data validation
pydantic==2.7.1

# Testing
pytest==8.2.2
httpx==0.27.0          # needed by fastapi TestClient

# Optional: used by pydub for non-WAV format support
# ffmpeg must also be installed on the system (see README)
